<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowTranslucentStatus">true</item>
        <item name="android:windowTranslucentNavigation">true</item>
        <item name="colorPrimary">@android:color/transparent</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="status_bar" parent="Theme.AppCompat.Light">
        <item name="colorPrimary">#FF00FF</item>
        <item name="colorPrimaryDark">#FF033E</item>
        <item name="colorAccent">#000000</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="statusBarBackground">@android:color/holo_blue_dark</item>
        <!--set marshmallow-->
        <item name="android:statusBarColor" tools:targetApi="m">#FFFFFF</item>
        <item name="android:windowLightStatusBar" tools:targetApi="m">true</item>
    </style>

    <!--styles for dialog boxes-->
    <style name="CustomDialog" parent="android:Theme.DeviceDefault.Dialog">
        <item name="DialogTitleAppearance">@android:style/TextAppearance.Medium</item>
        <item name="DialogSpotColor">@android:color/holo_blue_dark</item>
        <item name="DialogSpotCount">5</item>
    </style>

    <style name="noInternetDialog" parent="Theme.AppCompat.Light.Dialog.Alert">
    <item name="android:windowBackground">@drawable/buttons</item>
    <item name="android:textAlignment">center</item>
    <item name="android:textColorPrimary">#ffffff</item>
    <!--item name="android:background">@android:color/transparent</item-->
    <item name="android:backgroundDimEnabled">false</item>
    <item name="android:paddingTop">0dp</item>
    <!--item name="android:paddingLeft">10dp</item>
    <item name="android:paddingRight">10dp</item--><!--this adds padding to text-->
    <item name="android:layout_marginTop">0dp</item>
    </style>

    <style name="AlertDialog" parent="ThemeOverlay.AppCompat.Dialog">
        <item name="android:windowBackground">@drawable/rectanglebg</item>
        <!--item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item-->
        <item name="android:textAlignment">center</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="MyThemeOverFlow">
        <item name="android:src">@drawable/tab_color_new</item>
    </style>

    <style name="CustomToolbarStyle" parent="Theme.AppCompat.Light.NoActionBar" >
        <item name="android:background">#FFFFFF</item>
        <!--item name="android:actionBarItemBackground">@drawable/tab_color</item>
        <item name="actionBarItemBackground">@drawable/tab_color</item-->
    </style>

    <style name="CustomPopupStyle" parent="Widget.AppCompat.PopupMenu.Overflow">
        <item name="overlapAnchor">false</item>
        <item name="android:dropDownVerticalOffset">-5dp</item>
        <!--item name="android:dropDownVerticalOffset">?attr/actionBarSize</item-->
    </style>

</resources>
